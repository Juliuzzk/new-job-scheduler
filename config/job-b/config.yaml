# Configuración para Job B
database:
  host: localhost
  port: 5432
  name: jobscheduler_db
  user: admin
  password: admin123

api:
  endpoint: https://api.example.com
  key: secret-key-12345
  timeout: 30000
  retries: 5

job-b:
  enabled: true
  name: "API Integration Job"
  buffer-size: 256
  chunk-size: 1000
  notify-email: admin@example.com
